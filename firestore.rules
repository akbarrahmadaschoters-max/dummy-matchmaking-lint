rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Memastikan hanya user yang sudah terotentikasi (login) yang bisa akses
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
